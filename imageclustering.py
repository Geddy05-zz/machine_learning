from dbscan.dbScan import DBScan

# from PIL import Image

class Image_clustering:

    def get_image(self):
        print("a")
        # image_array = []
        # im = Image.open("mario.jpeg")
        # im.show()
        # temp = list(im.getdata())
        # print(len(temp))
        # for item in temp:
        #     image_array.append([item[0],item[1],item[2]])
        # db = DBScan("dbscan.csv", 300)
        # db.data = image_array
        # db.clustering(min_points= 30)

        # for value in db.clusters:
        #     print (" Cluster: %d" % value.name)
        #     for item in value.points:
        #         print(item)
        #     print("")